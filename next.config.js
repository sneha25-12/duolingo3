// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
 // experimental: {
  //  typedRoutes: true,
  //},
  // Remove all turbo/webpack configs
};

module.exports = nextConfig;
